/* globals: describe, it */
define([
], function () {
    describe( 'Singly Link List', function() {
        it( 'inserts new node at start', function() {
            expect( 'Hello' ).toEqual( 'Hello' );
        } );
        it( 'inserts new node at end', function() {
            expect( 'Hello' ).toEqual( 'Hello' );
        } );
        it( 'inserts new node after specific node', function() {
            expect( 'Hello' ).toEqual( 'Hello' );
        } );
    } );
});
